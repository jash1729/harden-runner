spring:
  application:
    name: owasp

  profiles:
    active: vulnerable
    
  datasource:
    url: jdbc:h2:file:./data/owasp
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  sql:
    init:
      mode: always